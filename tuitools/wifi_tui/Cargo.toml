[package]
name = "wifi_tui"
version = "0.1.0"
edition = "2024"

[dependencies]
ratatui = "0.26" # Современная библиотека для TUI
crossterm = "0.27" # Для работы с терминалом
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

