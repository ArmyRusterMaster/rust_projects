[package]
name = "grpc-project"
version = "0.1.0"
edition = "2021"  # Вместо "2024" — такой версии пока нет

[dependencies]
prost = "0.11"
tokio = { version = "1.37", features = ["full"] }
tonic = "0.8"

[build-dependencies]
tonic-build = "0.8"

